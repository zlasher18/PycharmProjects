from cvxopt import matrix, solvers


# solve the QP problem
P = matrix([ [1.0, 0.0], [0.0, 1.0] ])
q = matrix([0.0, -6.0])
G = matrix([[7.0],[-2.0]])
h = matrix([14.0])

sol=solvers.qp(P, q, G, h)
print(sol['x'])
